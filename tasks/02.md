Claude, o código do projeto deve ser extremamente limpo, brutalista e autoexplicativo, sem a presença de comentários poluindo os arquivos de desenvolvimento.

Por favor, execute as duas tarefas abaixo:

1. Limpeza de Código: Varra todos os arquivos de código-fonte dentro do diretório `src/` (incluindo `.ts`, `.tsx`, `.js` e `.css`) e remova todos os comentários de linha (`//`) e comentários de bloco (`/* ... */`). A única exceção que deve ser mantida são as diretivas obrigatórias do framework, como `'use client';`.
2. Atualização de Regras: Abra o arquivo `.claude/rules` (ou a documentação de regras base que estamos usando) e adicione uma nova diretriz obrigatória:
   "NÃO UTILIZE COMENTÁRIOS: É expressamente proibido gerar, inserir ou manter comentários no código-fonte (seja inline ou em bloco). O código deve ser limpo e autoexplicativo através da nomenclatura semântica de variáveis, componentes e funções."

Me avise quando todos os arquivos estiverem limpos e a regra estiver salva.
