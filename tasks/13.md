Claude, precisamos tornar o componente `CustomBoot.tsx` (e seus subcomponentes, caso já tenha dividido) 100% responsivo, garantindo que ele caiba perfeitamente em telas mobile sem vazar (overflow horizontal) e se expanda corretamente no desktop.

Por favor, refatore as classes Tailwind focando em uma abordagem mobile-first:

1. TIPOGRAFIA E CONTAINER:

- Reduza a fonte base no mobile e restaure no desktop: Altere o container principal de `text-[11px] p-8` para `text-[9px] sm:text-[11px] p-4 sm:p-8 overflow-x-hidden overflow-y-auto`.
- Garanta que textos muito longos tenham `break-words` ou `whitespace-normal` caso precisem quebrar linha no mobile.

2. QUEBRA DOS GRIDS (Mobile -> Desktop):

- Top Section (Informações de Boot e Logo): Mude de `grid-cols-2` para `grid-cols-1 lg:grid-cols-2`.
- Mid Section (Band Members): O grid com os integrantes deve mudar de `grid-cols-4` para `grid-cols-1 sm:grid-cols-2 xl:grid-cols-4`.
- Bottom Section (Virus Scan): Mude de `grid-cols-2` para `grid-cols-1 lg:grid-cols-2`.

3. AJUSTES DOS CAIXOTES:

- Na caixa de "WELCOME TO THE SYSTEM", altere o tamanho fixo de `w-60` para `w-full lg:w-60`.
- O tamanho das imagens SVG (Logos e Caveiras) deve encolher proporcionalmente no mobile (ex: `h-20 lg:h-32` para o Eskoria Logo, `w-16 h-16 lg:w-24 lg:h-24` para as caveiras).
- No Footer (Fase 5), a linha que contém o "SYSTEM STATUS: CRITICAL" e o "PRESS ANY KEY..." deve empilhar no celular: mude o container de flex direto para `flex flex-col sm:flex-row justify-between gap-2`.

4. REGRAS ABSOLUTAS:

- Não altere em NADA a cronologia (`setTimeout`, `startDelay`) e a lógica da máquina de estados. O tempo deve permanecer milimetricamente igual.
- Apenas modifique as strings do `className` relativas ao layout.
- É ESTRITAMENTE PROIBIDO gerar comentários no código (`//`, `/* */`, `{/* */}`). Entregue o código puramente funcional e limpo.

O scroll da tela deve ser automatico, de acordo com a quantidade de linhas na tela como em verdadeiro terminal
