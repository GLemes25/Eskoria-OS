Claude, o "Eskoria OS" é uma aplicação de estética hacker/cyberpunk estritamente escura. O suporte a "Light Mode" (tema claro) deve ser completamente extirpado do projeto.

Por favor, execute as seguintes tarefas rigorosamente sem adicionar nenhum comentário no código gerado:

1. Arquivo de Estilos (`src/app/globals.css`): Localize a diretiva `@layer base`. Copie todas as variáveis CSS que estão dentro do escopo `.dark` e cole-as dentro do escopo `:root`, substituindo as variáveis de tema claro. Após isso, você pode deletar o bloco `.dark` por completo. O padrão global (`:root`) agora deve ser o tema escuro.
2. Layout Principal (`src/app/layout.tsx`): Remova qualquer provedor de tema dinâmico (como `next-themes` ou `ThemeProvider`) se existir. Force a classe `dark` e um fundo preto na tag HTML raiz, deixando-a assim: `<html lang="pt-BR" className="dark bg-black">`.
3. Limpeza de Toggles: Faça uma busca rápida e delete qualquer componente de "ThemeToggle" ou botão de alternância de modo claro/escuro que possa ter sido gerado pelo shadcn/ui.

Me avise quando o ambiente estiver travado exclusivamente no modo escuro.
