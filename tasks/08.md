Claude, vamos popular o desktop do "Eskoria OS" com vários atalhos de sistema, jogos clássicos e aplicativos. No momento, a pasta `public/shortcuts` (ou `src/assets/shortcuts`, verifique o seu contexto) contém vários arquivos SVG com nomes inconsistentes e erros de digitação.

Sua tarefa é dividida em duas partes:

**PARTE 1: Padronização e Correção de Arquivos**
Execute comandos no terminal para renomear os arquivos SVG originais para um padrão `kebab-case.svg` limpo, corrigindo os seguintes erros:

1. `Ares.svg` -> `ares.svg`
2. `blind_Korn.mp3.svg` -> `blind-korn.svg`
3. `Controller Pnael.svg` -> `control-panel.svg` (Corrigindo "Pnael")
4. `Counter-Strike 1.6.svg` -> `cs-16.svg`
5. `Doom.svg` -> `doom.svg`
6. `GTA San Andreas.svg` -> `gta-sa.svg`
7. `Haldf-life.svg` -> `half-life.svg` (Corrigindo "Haldf")
8. `Internet Exporer.svg` -> `internet-explorer.svg` (Corrigindo "Exporer")
9. `Lixeira.svg` -> `lixeira.svg`
10. `MSN Messanger.svg` -> `msn.svg` (Corrigindo "Messanger")
11. `My Computer.svg` -> `my-computer.svg`
12. `Notes.svg` -> `notepad.svg`
13. `Paint.svg` -> `paint.svg`
14. `Pinball.svg` -> `pinball.svg`
15. `speedy eggbert.svg` -> `speedy-eggbert.svg`
16. `System32.svg` -> `system32.svg`
17. `Utorretnt.svg` -> `utorrent.svg` (Corrigindo "Utorretnt")
18. `Winrar.svg` -> `winrar.svg`

**PARTE 2: Criação da Estrutura de Dados (JSON)**
Após renomear os arquivos, crie um arquivo em `src/data/shortcuts.json` (crie a pasta `data` se não existir). Este arquivo deve conter um array de objetos JSON exportando a lista de atalhos.
A estrutura de cada objeto deve ser:
`{ "id": string, "name": string, "icon": string, "type": "system" | "app" | "game" | "file" }`

Regras para a criação do JSON:

- A propriedade `icon` deve apontar para o novo caminho corrigido (ex: `/shortcuts/doom.svg`).
- A propriedade `name` será o rótulo exibido na área de trabalho.
- Adicione a extensão `.exe` no `name` para todos os softwares e jogos (ex: `Ares.exe`, `Doom.exe`, `CS 1.6.exe`, `uTorrent.exe`, `Internet Explorer.exe`, `WinRAR.exe`, `MSN Messenger.exe`, `Paint.exe`, `Notepad.exe`).
- O arquivo de música deve se chamar `blind_Korn.mp3`.
- Pastas ou áreas de sistema como `Lixeira`, `My Computer`, `Control Panel` e `System32` NÃO DEVEM receber a extensão `.exe`.
- Não inclua comentários no arquivo JSON.

Me avise quando os arquivos estiverem renomeados e o JSON estiver criado para que possamos mapeá-los no `Desktop.tsx`.
