Claude, precisamos refatorar o arquivo `Desktop.tsx` modularizando a barra de tarefas e os ícones da área de trabalho, além de integrar os dados do JSON recém-criado.

Por favor, execute as seguintes tarefas em ordem:

1. Criação do Componente de Atalho (`src/app/components/Shortcut.tsx`):
   Crie um componente que receba as props `id`, `name`, `icon`, `isActive`, `onClick` e `onDoubleClick`. A estilização deve emular um ícone de Windows XP:

- Container em coluna com largura fixa (w-20 ou w-24).
- O ícone (tag `<Image>`) deve ter uma div sobreposta no canto inferior esquerdo contendo a clássica "setinha" de atalho (um quadrado branco com borda preta e uma seta preta).
- O texto (`name`) deve ficar abaixo do ícone, com tamanho `text-[12px]`, centralizado e com `leading-tight`.
- Estilo Inativo: Texto branco com um forte `text-shadow` preto imitando a borda dura do XP (`1px 1px 0 #000, -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000`).
- Estilo Ativo (`isActive`): O fundo do ícone e do texto recebe a cor de destaque (ex: `bg-kat-accent/40`) e o texto perde a sombra pesada.

2. Criação do Componente da Barra de Tarefas (`src/app/components/Taskbar.tsx`):
   Mova o bloco JSX inteiro que compõe a barra inferior (a div `h-10 w-full shrink-0...`) do `Desktop.tsx` para este novo arquivo. O componente deve aceitar as props `time` (string) e `isGlitching` (boolean). Transfira todas as importações de ícones SVG e `<Image>` referentes à barra para cá.

3. Refatoração do `Desktop.tsx`:

- Importe o arquivo JSON de atalhos (`import shortcutsData from "@/data/shortcuts.json"`). Se o caminho for diferente, ajuste automaticamente.
- Substitua o estado genérico `iconActive` por `activeShortcut` (armazenando o `id` do atalho selecionado, ou `null`). Ao clicar no fundo do Desktop, sete como `null`.
- Substitua o código estático do atalho antigo ("K.A.T. CONNECTION.EXE") por um `.map()` iterando sobre o `shortcutsData`. O container que envolve o `.map()` deve usar `flex flex-col flex-wrap content-start gap-4` com altura máxima amarrada à tela, para que os ícones formem colunas verticais (estilo grid clássico de desktop).
- No `.map()`, renderize o novo componente `<Shortcut />`, passando as props adequadas.
- O evento `onDoubleClick` de qualquer atalho com o tipo "system" (ou id referente ao KAT) deve abrir o terminal (`setTerminalOpen(true)`).
- Importe e renderize o componente `<Taskbar time={time} isGlitching={isGlitching} />` no final do Desktop.

REGRA ABSOLUTA: É expressamente proibido gerar qualquer tipo de comentário de código (`//` ou `/* */`) em qualquer um dos arquivos criados ou modificados.
