Implemente o design desta barra de tarefas do Figma:
@https://www.figma.com/design/fIbRQnwuYMtNW2szhqfQ82/Untitled?node-id=5-118&m=dev

Sua tarefa é atualizar o arquivo `Desktop.tsx`.

REGRAS ESTABELECIDAS (LEIA COM ATENÇÃO):

1. ESCOPO ESTRITO: Você NÃO DEVE alterar NADA do começo do arquivo. Mantenha todos os imports, states (`isGlitching`, `time`, etc), `useEffects`, o fundo (backgrounds/meshes), os ícones do desktop e as janelas (LoginForm, ProtocolSelection, SystemFailureWindow).
2. O ALVO: Vá até o final do componente `Desktop`. Atualmente, há um bloco de barra de tarefas bagunçado (com duas divs de barra aninhadas `h-12 w-full...`). Delete ESSE BLOCO INFERIOR por completo.
3. A SUBSTITUIÇÃO: No lugar do bloco deletado, insira o código gerado a partir do nó do Figma.
4. INTEGRAÇÃO DE LÓGICA: O novo design gerado pelo Figma DEVE utilizar as variáveis de estado já existentes.
   - Aonde houver um relógio no design, use a variável `{time}`.
   - Preservar as condicionais de glitch: injete classes como `${isGlitching ? "text-kat-error animate-[pulse_0.1s_infinite] -translate-x-1.25" : ""}` nos lugares apropriados da nova barra.
5. REGRA DE COMENTÁRIOS: É estritamente proibido gerar qualquer comentário (`//` ou `/* */`) no código final.
