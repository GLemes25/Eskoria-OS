Claude, precisamos transformar o final da aplicação KAT em uma experiência de vídeo cinematográfica. O componente `CinematicReveal` deve exibir um vídeo em tela cheia com reprodução automática em vez de uma imagem estática. Ao finalizar o vídeo, o sistema deve reiniciar automaticamente.

Por favor, refatore o componente `CinematicReveal.tsx` aplicando as seguintes alterações:

1. SUBSTITUIÇÃO DE MÍDIA:

- Remova a constante `REVEAL_IMAGE_URL` e o import do componente `Image` do Next.js.
- Substitua a tag `<Image />` pela tag HTML nativa `<video>`.

2. CONFIGURAÇÃO DO VÍDEO:

- O vídeo deve usar a constante `REVEAL_VIDEO_URL` no atributo `src`.
- Adicione as propriedades essenciais: `autoPlay`, `playsInline` e desabilite os controles (não adicione a propriedade `controls`).
- É crucial adicionar o evento `onEnded={onReboot}`. Isso fará com que o desktop seja recarregado automaticamente assim que o vídeo terminar.
- As classes do Tailwind para o vídeo devem garantir que ele cubra toda a tela sem distorcer: `className="object-cover object-center w-full h-full"`.

3. MANUTENÇÃO DE ESTILO:

- Mantenha a div pai com `absolute inset-0 z-200 bg-kat-bg overflow-hidden`.
- Mantenha a camada de gradiente (`bg-linear-to-b`) por cima do vídeo para dar aquele tom sombrio.
- Mantenha o botão "[ REBOOT_SYSTEM ]" no canto inferior direito. Ele servirá como um botão de "Skip" caso o usuário queira pular o vídeo antes dele terminar sozinho.

REGRAS ABSOLUTAS:

- Apenas implemente o código funcional.
- É ESTRITAMENTE PROIBIDO gerar comentários explicativos no código (`//`, `/* */`).
