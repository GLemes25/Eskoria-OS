### 💻 Comando para o Claude Code / Cursor

````text
Claude, precisamos aprimorar a Phase 0 do `BootManager`. O texto estático deve ser substituído por um efeito rápido de "máquina de escrever" (typewriter) e o conteúdo deve ser alterado para Inglês, simulando um log real de kernel Linux.

Por favor, execute as seguintes alterações no arquivo:

1. Criação do Micro-Componente:
Crie um componente chamado `TypewriterText` (pode ser no mesmo arquivo do BootManager, fora do componente principal).
- Props: `text` (string), `speed` (number, default 15) e `startDelay` (number, default 0).
- Lógica: Use um estado `displayedText` iniciando vazio. Use um `useEffect` com um `setTimeout` inicial baseado em `startDelay`. Após o delay, use um `setInterval` baseado na `speed` para adicionar um caractere por vez de `text` em `displayedText`.
- Retorno: `<p className="text-white text-sm leading-tight opacity-80">{displayedText}</p>`.
- Limpe os timers (`clearTimeout` e `clearInterval`) no cleanup do `useEffect`.

2. Substituição na Phase 0:
Substitua o bloco atual estático (`<div className="p-6">...</div>`) pela utilização do novo componente:
```tsx
<div className="p-6 flex flex-col gap-1">
  <TypewriterText text="Initializing K.A.T. Kernel..." speed={10} startDelay={0} />
  <TypewriterText text="Loading base modules..." speed={10} startDelay={400} />
</div>

````

REGRA ESTABELECIDA: Você está proibido de gerar qualquer comentário no código (`//`, `/* */` ou `{/* */}`). Retorne apenas o código atualizado e funcional.

```

### O que isso vai fazer:
* **`speed={10}`**: Faz a digitação ser quase instantânea (10 milissegundos por letra), imitando o console jogando texto rápido na tela.
* **`startDelay={400}`**: Garante que a segunda linha só comece a ser "digitada" 400 milissegundos depois, dando a sensação de que o sistema leu a primeira linha antes de processar a segunda.

```
