Claude, preciso fazer uma atualização global de UI em todos os componentes de janela/modal que ficam dentro do diretório `src/app/kat/` (ex: LoginForm, ProtocolSelection, SystemFailureWindow e similares).

O objetivo é aumentar a escala visual dessas interfaces e mudar o tom de alerta da sombra externa para vermelho.

Por favor, analise TODOS os arquivos dentro de `src/app/kat/` e execute as seguintes refatorações nas `divs` que servem como o container principal da janela:

1. AUMENTO DE ESCALA (SIZE):

- Aumente substancialmente a largura (width) base e máxima das janelas. Onde houver larguras como `w-175`, `max-w-md`, ou `max-w-2xl`, substitua por classes maiores, como `w-[800px] max-w-[95vw]` ou `w-[1000px]`.
- Se houver `p-6` ou `p-8` nas áreas internas de conteúdo, aumente o padding interno para dar respiro (ex: `p-10`).
- A altura máxima deve continuar respeitando os limites da tela (ex: `max-h-[90dvh]`), mas permita que a janela ocupe mais espaço vertical se o conteúdo interno exigir.

2. TROCA DE SOMBRA (GLOW VERMELHO):

- Localize a classe de box-shadow que gera o brilho ao redor da janela. Normalmente está configurada com algo semelhante a `shadow-[0_4px_30px_var(--kat-accent-glow-sm)]` ou usando cores da variável `kat-accent`.
- Substitua ESTRITAMENTE essa sombra por um brilho vermelho intenso e espalhado. Use a classe Tailwind: `shadow-[0_0_40px_rgba(239,68,68,0.4)]` (referência ao red-500 do Tailwind).
- NÃO altere as bordas (`border-kat-accent`) nem os textos internos para vermelho a menos que já estejam. A mudança é apenas na sombra (`shadow`).

3. REGRA ABSOLUTA DE TRANSPARÊNCIA:

- É mandatório que as janelas mantenham 90% de opacidade permanentemente. Se você for reescrever classes de background, certifique-se de que a janela principal use `bg-kat-bg/90` (ou equivalente a 90%). As janelas NÃO podem perder a transparência ou ficar 100% sólidas quando recebem foco (focus-within).

Entregue os arquivos refatorados sem gerar comentários (`//`, `/* */`) no código. Mantenha a lógica de estados e funções inalterada.

apliqwue sempre as regras de tailwind w use as cores do .css
