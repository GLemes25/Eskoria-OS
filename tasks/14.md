Claude, preciso modificar o comportamento dos atalhos no componente `Desktop.tsx`. Atualmente, ícones aleatórios disparam um `window.alert`. Quero substituir isso por uma imagem em tela cheia (overlay) que simula um erro ou tela assustadora, desaparecendo sozinha após 5 segundos.

Por favor, faça as seguintes refatorações no componente `Desktop`:

1. CRIAR O ESTADO DE AVISO:

- Adicione um novo estado: `const [showDontOpen, setShowDontOpen] = useState(false);`

2. LÓGICA DO TEMPORIZADOR (5 Segundos):

- Crie um `useEffect` que monitore o estado `showDontOpen`.
- Se `showDontOpen` for `true`, inicie um `setTimeout` de 5000ms que altera o estado de volta para `false`.
- Lembre-se de retornar a função de limpeza (`clearTimeout`).

3. ATUALIZAR O EVENTO DO SHORTCUT:

- Localize o `onDoubleClick` dentro do map de `shortcutsData`.
- Mantenha a regra: `if (shortcut.id === "eskoria-so") { setTerminalOpen(true); }`.
- No `else`, remova o `window.alert` e chame `setShowDontOpen(true);`.

4. RENDERIZAR O OVERLAY DA IMAGEM:

- No final do JSX (antes de fechar a div principal ou imediatamente acima da Taskbar), adicione a renderização condicional do overlay da imagem.
- A imagem deve sobrepor TUDO no desktop. Use o seguinte escopo de layout:

```tsx
{
  showDontOpen && (
    <div className="fixed inset-0 z-[9999] bg-black pointer-events-none flex items-center justify-center">
      <Image
        alt="DO NOT OPEN"
        className="object-cover object-center"
        fill
        priority
        src="/dontopen.png"
        unoptimized
      />
    </div>
  );
}
```
