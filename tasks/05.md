Claude, preciso substituir o plano de fundo estático do `LockScreen.tsx` pelo vídeo oficial da banda rodando em loop infinito (como um GIF). Além disso, precisamos configurar o Next.js para aceitar os assets do Cloudinary.

Por favor, execute as seguintes tarefas rigorosamente sem adicionar nenhum comentário no código gerado:

1. Modificação do Next Config: Abra o arquivo `next.config.ts` (ou `.js`) e configure a chave `images` com `remotePatterns` para permitir o `hostname: "res.cloudinary.com"`.
2. Substituição no LockScreen: No arquivo `src/components/os/LockScreen.tsx` (ou no caminho correspondente), localize a div de background (`absolute inset-0 z-0`) e substitua a tag `<Image>` por uma tag nativa `<video>`.
3. Comportamento do Vídeo: A tag `<video>` NÃO DEVE ter o atributo `controls`. Ela deve conter exclusivamente os atributos `autoPlay`, `loop`, `muted` e `playsInline`.
4. URL e Otimização: Adicione uma tag `<source>` dentro do vídeo apontando para esta URL exata: `https://res.cloudinary.com/dk7zfhbrj/video/upload/f_auto,q_auto/v1780000153/transitionlogoeskoria_zaoupz.mp4` (tipo `video/mp4`).
5. Estilização CSS: O vídeo deve receber as seguintes classes do Tailwind para se mesclar ao fundo brutalista: `w-full h-full object-cover opacity-40 mix-blend-luminosity pointer-events-none`.

Me avise quando concluir a configuração de domínio e a refatoração do componente.
