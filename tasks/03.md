Claude, ao longo do projeto você gerou um placeholder `<svg>` desenhado matematicamente (uma estrela com círculos e linhas, com viewBox="0 0 400 400") para representar a logo da banda.

No entanto, o meu arquivo oficial da logo já existe e está localizado em `public/logo.svg`.

Por favor, faça o seguinte:

1. Faça uma busca global nos arquivos do frontend (especialmente no `LockScreen.tsx`, `BootScreen.tsx` e qualquer outro componente onde a logo apareça).
2. Substitua todo o bloco `<svg> ... </svg>` do placeholder pela tag de imagem.
3. Se optar por usar o `<Image>` do Next.js, envolva-o em uma div com as classes de responsividade (`relative w-full h-[30dvh] ...`) e use a propriedade `fill` com `object-contain`.
