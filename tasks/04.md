Estamos com um bug no componente `LockScreen.tsx`. A tela está avançando ('entrando sozinha') ao receber qualquer clique do usuário.

Isso está acontecendo porque a div raiz (wrapper) do componente ainda possui a propriedade `onClick={onUnlock}` da versão antiga, ou porque há algum evento vazando.

Por favor, faça a seguinte correção no `LockScreen.tsx`:

1. Remova a propriedade `onClick={onUnlock}` da `div` principal/raiz do componente.
2. Certifique-se de que o `onUnlock()` seja chamado ÚNICA e EXCLUSIVAMENTE dentro da função de submissão do formulário (`handleLogin` / `onSubmit`), e apenas se a regra de validação da senha for atendida.
3. Garanta que a div raiz e outros elementos de fundo não interceptem cliques que acionem o login.

Me avise quando concluir a correção.
